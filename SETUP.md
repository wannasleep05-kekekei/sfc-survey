# はじめかた

プログラミングの経験がない方向けの導入手順です。GitHub の知識も要りません。
所要時間は 15〜20 分ほど（うち大半は Python のインストール待ち）。

---

## これは何をするもの？

先行研究を探すときの、こういう作業を肩代わりします。

- CiNii、国立国会図書館、OpenAlex などを**まとめて一度に検索**する
- 同じ論文が複数のサイトから出てきたら**1件にまとめる**
- **「この論文を引用している論文」を芋づる式に辿る**（これが一番強力です）
- 無料で読める本文は自動でダウンロードする
- 慶應に所蔵があるか確認するためのリストを作る

キーワード検索だけでは見つからない文献が出てくるのが最大の利点です。

---

## 手順は4つだけ

1. Python を入れる（初回のみ）
2. ファイルをダウンロードする
3. 「ターミナル」を開いて、自分のメールアドレスを設定する
4. 検索してみる

---

## Step 1：Python を入れる

このツールは Python というプログラムの上で動きます。

### まず、入っているか確認

すでに入っているかもしれないので、先に確かめます。Step 3 の「ターミナルの開き方」を
見て黒い画面を開き、次のように打って Enter を押してください。

```
python --version
```

`Python 3.10.5` のように数字が出れば**入っています。Step 2 へ進んでください。**

エラーが出たり何も出なければ、下の手順で入れます。

### Windows の場合

1. https://www.python.org/downloads/ を開く
2. 黄色い「Download Python 3.x.x」ボタンを押す
3. ダウンロードされたファイルを開く
4. **⚠ 最初の画面で「Add python.exe to PATH」に必ずチェックを入れてください**
   （これを忘れると後で動きません。一番間違えやすいところです）
5. 「Install Now」を押して待つ

### Mac の場合

Mac には最初から入っていることが多いです。`python3 --version` で確認してください。
無ければ https://www.python.org/downloads/ から同様にインストールします。

---

## Step 2：ファイルをダウンロードする

1. https://github.com/wannasleep05-kekekei/sfc-survey を開く
2. 緑色の **「Code」** ボタンを押す（ページ右上のあたり）
3. 出てきたメニューの一番下 **「Download ZIP」** を押す
4. `sfc-survey-main.zip` がダウンロードされます

### 展開して、置き場所を決める

ZIPファイルを右クリック →

- **Windows**：「すべて展開」
- **Mac**：ダブルクリックで展開されます

展開してできた **`sfc-survey-main`** というフォルダを、**デスクトップに移動**してください。
（どこでもいいのですが、後の手順が説明しやすいので）

フォルダを開くと、こういうファイルが入っていれば成功です。

```
README.md
sfc-search
sfc_search（フォルダ）
```

---

## Step 3：ターミナルを開く

「ターミナル」は文字でコンピュータに命令する画面です。黒い（または白い）画面が出ます。

### Windows の場合

1. `sfc-survey-main` フォルダを開く
2. **フォルダの上の方にある、アドレスが表示されている細長い欄**をクリックする
   （`PC > デスクトップ > sfc-survey-main` のように表示されている部分）
3. 表示が消えて入力できる状態になるので、`cmd` と打って Enter

黒い画面が開き、最後の行が `...\sfc-survey-main>` で終わっていれば成功です。

### Mac の場合

1. `sfc-survey-main` フォルダを**右クリック**
2. 「サービス」→「フォルダに新規ターミナル」

うまくいかない場合は、Launchpad から「ターミナル」を開いて、

```
cd 
```

と打ち（`cd` の後に**スペースを1つ**）、そこに `sfc-survey-main` フォルダを
**ドラッグ＆ドロップ**してから Enter。これでフォルダの場所が自動で入ります。

---

## Step 4：自分のメールアドレスを設定する

ターミナルに次のように打って Enter。**メールアドレスは自分のものに変えてください。**

```
python sfc-search config --contact 自分のアドレス@keio.jp
```

> **Mac の方は `python` ではなく `python3` と打ってください。**
> 以降のコマンドもすべて同じです。

こう表示されれば成功です。

```
連絡先を保存しました: ...
contact    : 自分のアドレス@keio.jp
User-Agent : sfc-search-tools/0.1 (+https://github.com/...; contact: 自分のアドレス@keio.jp)
```

### なぜメールアドレスが必要？

このツールは国立国会図書館などのサーバーに問い合わせをします。その際に
「誰が使っているか」を正直に名乗る設計になっています。

もし何か問題が起きたとき、名乗っていれば**いきなり遮断されるのではなく、
あなたに連絡が来ます**。また、いくつかのサービスは連絡先付きのリクエストを
優先的に処理してくれるので、少し速くなります。

**必ず自分のアドレスを入れてください。** 人から教わったものをそのまま使うと、
その人に連絡が行ってしまいます。

---

## 使ってみる

### まず検索

```
python sfc-search search "調べたいキーワード" --count 20
```

例：

```
python sfc-search search "視覚障害 情報アクセシビリティ" --count 20
```

しばらく待つと（1分ほどかかります）、関連度の高い順に文献が並びます。

> 表示されない・エラーが出る場合は、下の「困ったときは」を見てください。

### 引用を辿る（このツールの本領）

検索結果に `doi:10.1234/...` という番号が付いている論文があります。
重要そうな論文を1本選んで、次のように打ってください。

```
python sfc-search snowball 10.1234/abcd
```

その論文が**引用している文献**と、その論文を**引用している文献**がまとめて出ます。
キーワード検索では絶対に出てこない文献が見つかります。

### 一覧をファイルに書き出す

```
python sfc-search export --format md --with-abstract --out 調査結果.md
```

`調査結果.md` というファイルがフォルダにできます。抄録（要旨）付きの一覧です。

**このファイルを Claude や ChatGPT に読ませて「分類して」「必読を選んで」と頼むと、
絞り込みが一気に進みます。** ここが一番おすすめの使い方です。

### 本文を手に入れる

```
python sfc-search fetch --limit 20
```

無料で公開されている（オープンアクセスの）本文だけを自動で保存します。
`papers` というフォルダができて、その中に入ります。

有料の論文には手を出しません。それらは次のコマンドでブラウザから読んでください。

```
python sfc-search open 3
```

（`3` は一覧の番号です）

慶應の学内ネットワークか、KOSMOS 経由でログインすれば読めるものが多くあります。

### 慶應に所蔵があるか調べる

```
python sfc-search holdings
```

KOSMOS で検索するためのリンクと検索語が並びます。リンクを開いて、
**検索ボタンを押し直して**確認してください。
（KOSMOS の仕様上、自動で表示される結果は当てになりません）

---

## もっと楽をする：Claude Code

ここまでは自分でコマンドを打つ方法でした。

**Claude Code** というアプリを使うと、コマンドを打つ代わりに
**日本語で頼むだけ**になります。

> 「このツールを使って、成年被後見人の選挙権についての文献を30件集めて、
> 重要そうなものを10本に絞って」

と書けば、Claude が自分で検索し、引用を辿り、結果を読んで、絞り込んだ結果を
返してくれます。

このフォルダには `CLAUDE.md` という説明書が入っていて、Claude が自動で読みます。
やってはいけないことも書いてあるので、余計な心配は要りません。

導入は https://claude.com/product/claude-code から。

---

## 困ったときは

### `python` は認識されていません / command not found

Python が入っていないか、Windows で「Add python.exe to PATH」のチェックを
忘れています。Python をアンインストールして、**チェックを入れて**入れ直してください。

Mac の場合は `python` ではなく `python3` を試してください。

### そのようなファイルまたはディレクトリはありません

ターミナルが `sfc-survey-main` フォルダの中にいません。Step 3 をやり直してください。

今どこにいるかは次で確認できます。

```
cd
```
（Mac は `pwd`）

### 検索結果が0件

- キーワードを減らしてみてください（2語くらいが調度いいです）
- 日本語の書籍を探しているなら `--books` を付けてみてください
- それでも少なければ `--loose` を付けると範囲が広がります

```
python sfc-search search "参政権" --books --count 20
```

### 中国語の無関係な論文ばかり出てくる

日本語のキーワードで検索したのに、見たこともない中国語の論文が並ぶことがあります。
海外のデータベース（Crossref）が漢字に反応してしまうためで、故障ではありません。

日本語の文献を探しているなら、検索するソースを日本のものだけに絞ってください。

```
python sfc-search search "キーワード" --source cinii --source ndl
```

CiNii（日本語論文）と国立国会図書館（日本語書籍）だけを見にいくようになります。
日本語のテーマなら、最初からこれを付けて検索するのがおすすめです。

### 文字化けする

Windows の古いコマンドプロンプトで起きることがあります。
「Windows Terminal」（Microsoft Store から無料）を使うと直ります。

### エラーの意味が分からない

エラーメッセージをそのままコピーして、Claude や ChatGPT に貼り付けて
「これはどういう意味？」と聞くのが一番早いです。

---

## 知っておいてほしいこと

このツールは、**慶應のシステムに自動でアクセスしないよう意図的に作られています。**

大学の情報システム利用規程と、図書館が契約している電子ジャーナルの規約は、
機械による自動ログインや本文の大量ダウンロードを禁じています。違反が見つかると、
自分だけでなく**慶應全体でそのデータベースが使えなくなる**ことがあります。

そのため、このツールは

- 慶應のIDやパスワードを一切扱いません
- 有料の論文本文には触れません
- 相手のサーバーに負担をかけないよう、ゆっくり順番に問い合わせます

という作りになっています。少し遅く感じるかもしれませんが、これは仕様です。
速くしようとして設定を書き換えないでください。

有料の論文が必要なときは、`open` でブラウザから普通に読むか、`ill` で
メディアセンターに文献複写を依頼してください。

```
python sfc-search ill
```

---

## コマンド早見表

| やりたいこと | コマンド |
|---|---|
| 検索する | `python sfc-search search "キーワード" --count 20` |
| 書籍だけ探す | `python sfc-search search "キーワード" --books` |
| 引用を辿る | `python sfc-search snowball <DOI>` |
| 一覧を書き出す | `python sfc-search export --format md --with-abstract --out 結果.md` |
| 無料本文を保存 | `python sfc-search fetch --limit 20` |
| ブラウザで開く | `python sfc-search open 3` |
| 慶應の所蔵を調べる | `python sfc-search holdings` |
| 複写依頼リスト | `python sfc-search ill` |
| ヘルプを見る | `python sfc-search --help` |

（Mac は `python` を `python3` に読み替えてください）
